# 🔴 Embedding Explorer

Образовательный веб-инструмент для демонстрации работы эмбеддингов и косинусного сходства в контексте RAG-систем.

## 📋 Описание

Embedding Explorer — это интерактивное приложение для изучения того, как текст преобразуется в векторы (эмбеддинги) и как измеряется семантическая близость между фразами с помощью косинусного сходства.

### Основные возможности

- ✅ Добавление фраз и получение их векторных представлений
- ✅ Вычисление косинусного сходства между двумя фразами
- ✅ Визуализация результатов с интерпретацией
- ✅ Поддержка двух провайдеров эмбеддингов: **Ollama** (локальный) и **OpenAI**
- ✅ Отображение векторов (первые 20 измерений)
- ✅ Справочная информация о эмбеддингах и косинусном сходстве
- ✅ Тёмная тема с красными акцентами

## 🚀 Быстрый старт

### Предварительные требования

**Для использования Ollama (рекомендуется для начала):**

```bash
# Установите Ollama (если ещё не установлен)
# https://ollama.ai/

# Загрузите модель BGE-M3
ollama pull bge-m3

# Запустите Ollama сервер
ollama serve
```

**Для использования OpenAI:**

Получите API ключ на [platform.openai.com](https://platform.openai.com)

### Установка и запуск

```bash
# Клонируйте репозиторий (или перейдите в папку проекта)
cd embeddings_demo

# Установите зависимости
npm install

# (Опционально) Создайте .env файл для API ключей
cp .env.example .env
# Отредактируйте .env и добавьте ваш OpenAI API ключ

# Запустите dev сервер
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 🎯 Как использовать

1. **Настройте провайдер** — нажмите на иконку настроек (⚙️) в правом верхнем углу
2. **Выберите провайдер** — Ollama (локальный) или OpenAI
3. **Добавьте фразы** — введите текст в поле ввода и нажмите "Добавить"
4. **Выберите две фразы** — отметьте чекбоксы у двух фраз для сравнения
5. **Изучите результат** — косинусное сходство отобразится автоматически

### Примеры фраз для тестирования

**Высокое сходство:**
- "Кошка сидит на окне" ↔ "Кот лежит у окна"
- "Программирование на Python" ↔ "Разработка на языке Python"

**Низкое сходство:**
- "Кошка сидит на окне" ↔ "Автомобиль едет по дороге"
- "Программирование на Python" ↔ "Приготовление пиццы"

## 🛠 Технологический стек

- **Framework:** React 18 + TypeScript
- **Build Tool:** Vite
- **UI Library:** shadcn/ui
- **Styling:** Tailwind CSS
- **Icons:** Lucide React
- **Notifications:** Sonner
- **State Management:** React useState/useEffect

## 📁 Структура проекта

```
embeddings_demo/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui компоненты
│   │   ├── Header.tsx       # Шапка приложения
│   │   ├── PhraseInput.tsx  # Поле ввода фразы
│   │   ├── PhraseList.tsx   # Список фраз
│   │   ├── PhraseItem.tsx   # Элемент списка
│   │   ├── SimilarityDisplay.tsx  # Отображение результата
│   │   ├── SettingsDialog.tsx     # Настройки провайдера
│   │   └── InfoBlock.tsx    # Справочная информация
│   ├── lib/
│   │   ├── embeddings/
│   │   │   ├── ollama.ts    # Клиент Ollama API
│   │   │   ├── openai.ts    # Клиент OpenAI API
│   │   │   └── index.ts     # Фабрика провайдеров
│   │   ├── similarity.ts    # Косинусное сходство
│   │   └── utils.ts         # Утилиты
│   ├── types/
│   │   └── index.ts         # TypeScript типы
│   ├── App.tsx              # Главный компонент
│   ├── main.tsx             # Точка входа
│   └── index.css            # Глобальные стили
├── docs/
│   └── requirements.md      # Техническое задание
├── .env.example             # Пример переменных окружения
└── README.md                # Этот файл
```

## 🔧 Доступные провайдеры

### Ollama (локальный)

- **Модель:** `bge-m3`
- **Размерность:** 1024
- **Преимущества:** Бесплатный, работает локально, не требует API ключа
- **Требования:** Установленный Ollama

### OpenAI

- **Модели:**
  - `text-embedding-ada-002` (1536 измерений)
  - `text-embedding-3-small` (1536 измерений)
  - `text-embedding-3-large` (3072 измерения)
- **Преимущества:** Высокое качество, не требует локальной установки
- **Требования:** API ключ OpenAI

## 📊 О косинусном сходстве

Косинусное сходство измеряет угол между двумя векторами:

```
cos(A, B) = (A · B) / (||A|| × ||B||)
```

**Значения:**
- `1.0` — идентичные векторы (0°)
- `0.0` — ортогональные векторы (90°)
- `-1.0` — противоположные векторы (180°)

**Интерпретация:**
- `≥ 0.95` — Идентичные
- `≥ 0.70` — Высокое сходство
- `≥ 0.40` — Среднее сходство
- `≥ 0.10` — Низкое сходство
- `< 0.10` — Противоположные значения

## 🎓 Образовательные цели

Этот проект создан для демонстрации:

1. **Эмбеддингов** — как текст преобразуется в числовые векторы
2. **Косинусного сходства** — как измеряется семантическая близость
3. **RAG-систем** — как эмбеддинги используются для поиска релевантной информации
4. **Semantic Search** — поиск по смыслу, а не по ключевым словам

## 🔐 Безопасность

- API ключи сохраняются только в `localStorage` браузера
- История фраз **не сохраняется** при перезагрузке страницы
- Все вычисления происходят на клиенте

## 📝 Лицензия

Образовательный проект для курса по n8n и RAG-системам.

## 🤝 Поддержка

Если возникли проблемы:

1. **Ollama не запускается** — проверьте, что Ollama сервер запущен (`ollama serve`)
2. **Модель не найдена** — выполните `ollama pull bge-m3`
3. **OpenAI ошибка** — проверьте правильность API ключа
4. **Сетевая ошибка** — проверьте подключение к интернету

## 🚧 Возможные расширения (v2)

- Визуализация векторов через t-SNE/UMAP
- Матрица сходства для всех пар фраз
- Экспорт результатов в CSV
- Сохранение сессий
- Поддержка других провайдеров (Cohere, Voyage AI)
- Batch-ввод нескольких фраз
